template
    require(from='./assets/css/folder-type.css')
    .folder-type.file-manager-item
        .item.folder(data-path='${this.path}', click.trigger='this.toFolder')
        .itemName ${this.name}
